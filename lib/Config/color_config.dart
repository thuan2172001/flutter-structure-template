import 'package:flutter/material.dart';
import 'package:flutter_auth/Services/Utility/material-color-helper.dart';

class ColorConfig {
  ColorConfig._();

  static MaterialColor primaryColor = MaterialColorHelper.createMaterialColor(Color(0xFF37B4EE));
  static MaterialColor darkColor = MaterialColorHelper.createMaterialColor(Color(0xffffcdd2));
  static MaterialColor secondaryColor = MaterialColorHelper.createMaterialColor(Color(0xFFE3F2FD));
  static MaterialColor thirdColor = MaterialColorHelper.createMaterialColor(Color(0xFF37B4EE));
}